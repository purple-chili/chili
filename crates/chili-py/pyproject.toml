[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[project]
name = "chili"
version = "0.8.0"
description = "Python bindings for the Chili engine (EngineState)"
requires-python = ">=3.10"

[tool.maturin]
module-name = "chili.engine_state"
python-source = "."
